#include <Arduino.h>
#include <Servo.h>

// ================= CONFIGURATION =================

// TODO(Student): Declare Servo object for MG995
// Hint: Use Servo class
// Servo myServoMG995;

// TODO(Student): Declare servo control pin
// Hint: Use PWM pin (e.g., 9)
// int servoPin = ?;

// =================================================

void setup() {
    // TODO(Student): Initialize Serial communication at 9600 baud

    // TODO(Student): Attach MG995 servo to the declared pin

    // TODO(Student): Print "MG995 Servo Initialized" on Serial Monitor
}

void loop() {

    // TODO(Student):
    // Rotate servo from 0° to 180°
    // Hint:
    // - Use for loop
    // - Step size = 10
    // - Use servo.write(angle)
    // - Print angle on Serial Monitor
    // - Add delay of 500 ms

    // TODO(Student):
    // Rotate servo back from 180° to 0°
    // Hint:
    // - Reverse for loop
}
